#include <iostream>

using namespace std;

int main(){
	int op;
	double nota1, nota2, media;

	cin >> nota1;
	if(nota1 >=0 && nota1 <=10){
		cin >> nota2;
		if(nota2 >=0 && nota2 <=0){
			media=(nota1+nota2)/2;
			cout << "media = " << fixed << setprecision(2) << media << endl
			}
		}
	}

}